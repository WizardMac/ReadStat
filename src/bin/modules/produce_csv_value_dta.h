#ifndef __PRODUCE_CSV_VALUE_DTA_H
#define __PRODUCE_CSV_VALUE_DTA_H

#include "../../readstat.h"
#include "produce_csv_column_header.h"

void produce_csv_value_dta(const char *s, size_t len, struct csv_metadata *c);

#endif
